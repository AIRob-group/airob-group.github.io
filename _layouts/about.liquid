---
layout: default
---
<div class="post">
  <header
    class="post-header text-center"
    style="background: linear-gradient(rgba(255,255,255,0.9), rgba(255,255,255,0.9)), url('/assets/img/AIRob.png'); background-size: cover; background-position: center; background-repeat: no-repeat; padding: 4rem 2rem;"
  >
    <h1 class="post-title" style="font-size: 6rem;">
      AI<img
        src="/assets/img/R_robot.png"
        alt=""
        style="height: 1.2em; vertical-align: 0em;"

      >ob Lab
    </h1>
    <p class="desc">
      <em>{{ page.subtitle }}</em>
    </p>
  </header>

  <article>
    <div class="clearfix">{{ content }}</div>

    <!-- News -->
    {% if page.announcements and page.announcements.enabled %}
      <h2>
        <a href="{{ '/news/' | relative_url }}" style="color: inherit">News</a>
      </h2>
      {% include news.liquid limit=true %}
    {% endif %}

    <!-- Selected papers -->
    {% if page.selected_papers %}
      <h2>
        <a href="{{ '/publications/' | relative_url }}" style="color: inherit">Recent Publications</a>
      </h2>
      <div class="publications">
        {% bibliography --group_by none --offset 1 --max 5 %}
      </div>
    {% endif %}

    <h3>Contact Information</h3>
    <p>
      <i class="fa-solid fa-house fa-fw"></i> <a href="https://roomfinder.sfu.ca/apps/sfuroomfinder_web/?q=TASC18005">TASC1 8005</a>, Simon Fraser
      University<br>
      <i class="fas fa-map-marker-alt fa-fw"></i> 8888 University Drive, Buraby, BC, Canada V5A 1S6<br>
      <i class="fa-solid fa-envelope fa-fw"></i> hangma at sfu dot ca
    </p>

    {% assign profile_image_class = 'img-fluid z-depth-1 w-25' %}
    {% capture sizes %}(min-width: {{ site.max_width }}) {{ site.max_width | minus: 30 | times: 0.3}}px, (min-width: 576px)
  30vw, 95vw"{% endcapture %}
    <div class="flex-column">
      <div class="flex-fill">
        {%
          include figure.liquid
          loading="eager"
          path='assets/img/logo_w_sfu.png'
          class=profile_image_class
          sizes=sizes
          alt=''
          cache_bust=true
        %}
      </div>
      <div class="flex-fill">
        {%
          include figure.liquid
          loading="eager"
          path='assets/img/sfuai_wide.png'
          class=profile_image_class
          sizes=sizes
          alt=''
          cache_bust=true
        %}
      </div>
    </div>

    <!-- Social -->
    {% if page.social %}
      <div class="social">
        <div class="contact-icons">{% include social.liquid %}</div>

        <div class="contact-note">{{ site.contact_note }}</div>
      </div>
    {% endif %}

    {% if site.newsletter and site.newsletter.enabled and site.footer_fixed %}
      {% include newsletter.liquid center=true %}
    {% endif %}
  </article>
</div>
